# Shared

Collaboration boundary for multi-agent work.

Keep task handoff artifacts here while agents still depend on these paths:
- `tasks/`
- `outputs/`
- `plans/`
- `logs/`
- `status/`
- `intel/`

Workspace doctrine and operational runbooks now live under `ops/`.
